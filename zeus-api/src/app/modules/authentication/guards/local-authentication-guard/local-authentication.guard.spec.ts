import { LocalAuthenticationGuard } from './local-authentication.guard';

describe('LocalAuthenticationGuardGuard', () => {
  it('should be defined', () => {
    expect(new LocalAuthenticationGuard()).toBeDefined();
  });
});
